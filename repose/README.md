# repose - relax position and energy using data driven potentials

## compilation

`repose` uses the compiled `spglib` in `airss`. In order to make
`repose`, do not `make clean` or `make neat` in `airss`.
